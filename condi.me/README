Managing kubernetes from terraform

Setup
-----

Secrets are store in password-store, note that provider secrets are sourced from
the environment, not the password-store provider.

    $ source <(pass consul.condi.me/terraform-plans)
    $ terraform init

Running
-------

    $ terraform plan
    $ terraform apply
